import React from 'react';
import ProfilePhoto from './1.jpg';

// functional component that returns photo profile
const Photo = () => {
  return <img src={ProfilePhoto} />;
};
export default Photo;
